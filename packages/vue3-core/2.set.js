// const effect = () => {}
// let set = new Set([effect])
// set.forEach((effect) => {
//   set.delete(effect)
//   set.add(effect)
// })

const arr = [1]
for (let i = 0; i < arr.length; i++)
  arr.push(i)
